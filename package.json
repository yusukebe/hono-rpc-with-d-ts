{
  "name": "hono-rpc-with-d-ts",
  "scripts": {
    "dev": "concurrently \"npm:dev:*\"",
    "dev:server": "bun run --watch server/server.ts",
    "dev:client": "tsc -p tsconfig.client.json --watch"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.2",
    "hono": "^4.6.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}